{
  "name": "portfolio-dev-server",
  "version": "1.0.0",
  "description": "Local dev server to verify Google ID tokens and serve the portfolio site.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
  "google-auth-library": "^8.8.0"
  }
}